import React from 'react';
import loader from './../../../assets/img/loader.svg'

const Preloader = (prpos) => {
  return (<div>
            <img src={loader}/>
          </div>
         )
};

export default Preloader;
